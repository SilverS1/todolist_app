<div class = "row">
	<div class = "col-md-6 col-md-offset-3">
		<%= form_for(@todo_list) do |f| %>
			
			<%= f.label :title %>
			<%= f.text_field :title, class: "form-control" %>
			
			<%= f.submit  "Create post", class: "btn btn-primary", data: {disable_with: "Creating..."} %> 
		
		<% end %>
	</div>
</div>